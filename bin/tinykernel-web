#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
REPO_ROOT="$(cd "${SCRIPT_DIR}/.." && pwd)"

echo "Iniciando Interface Web do TinyKernel (SisTer)..."
exec python3 "${REPO_ROOT}/web/serve.py" "$@"
